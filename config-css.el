;; config css
;; (require 'sass-mode)
;; (add-to-list 'auto-mode-alist '("\\.sass$" . sass-mode))

;;(setq exec-path (cons (expand-file-name "~/.gem/ruby/1.9.1/bin") exec-path))
;; (require 'scss-mode)
(add-to-list 'auto-mode-alist '("\\.scss$" . css-mode))


(require 'rainbow-mode)